# Setup workers for the application

development:
  app_name: <%= app_name %>
  force_kill_wait: 30
  workers:
  - name: job-fu-worker
      
test:
  app_name: <%= app_name %>
  force_kill_wait: 30
  workers:
  - name: job-fu-worker
            
production:
  app_name: <%= app_name %>
  force_kill_wait: 30
  workers:
  - name: job-fu-worker